{
  "$schema": "../gen/schemas/windows-schema.json",
  "identifier": "default",
  "description": "Minimal capability grant for HPN VPN on Windows. Exposes only the in-crate #[tauri::command] functions plus Tauri core defaults. Third-party plugins (shell) are NOT granted — if you add one, extend this allow-list explicitly.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "updater:default",
    "process:default"
  ]
}
